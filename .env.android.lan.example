# Copie para .env.android.lan — APK apontando para o PC na rede local (Wi‑Fi).
# Troque pelo IP da sua máquina (ipconfig). API deve escutar 0.0.0.0:3005.
# Build: npm run build:android:lan && (cd android && .\gradlew.bat assembleDebug)
VITE_API_URL=http://192.168.0.10:3005
