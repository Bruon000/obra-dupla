# Copie para .env.production (não commitar URLs com segredo se não quiser).
# URL pública da API (HTTPS), sem barra no final.
# Ex.: depois do deploy no Render / Railway / Fly.
VITE_API_URL=https://canteiro-api.onrender.com
