# Copie para .env.production na raiz (não commitar se preferires).
# Na Netlify: a mesma variável VITE_API_URL nas Environment variables (obrigatório para produção).
# URL pública da API (HTTPS), sem barra no final.
VITE_API_URL=https://obra-dupla.onrender.com
